<form method="post" action="{{ path('admin.contact_role.delete', {'id': contact_role.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_role.id) }}">
    <button class="btn">Delete</button>
</form>
