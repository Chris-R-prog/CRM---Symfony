<!DOCTYPE html>

<title>New ContactRole</title>

{% block body %}
    <h1>Create new ContactRole</h1>

    {{ include('admin/contact_role/_form.html.twig') }}

    <a href="{{ path('admin.contact_role') }}">back to list</a>
{% endblock %}
