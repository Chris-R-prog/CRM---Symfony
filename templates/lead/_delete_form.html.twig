<form method="post" action="{{ path('lead.delete', {'slug': lead.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lead.id) }}">
    <button class="btn">Delete</button>
</form>
